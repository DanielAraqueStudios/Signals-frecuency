fastapi==0.115.0
uvicorn[standard]==0.30.6
onnxruntime==1.19.2
pillow==10.4.0
numpy==1.26.4
paho-mqtt==2.1.0
pydantic-settings==2.5.2
pytest==8.3.3
httpx==0.27.2
python-multipart==0.0.9

# --- Training-only dependencies (NOT installed in the serving Docker image) ---
# model/train.py and model/export_onnx.py need a separate dev/training
# environment. Install these yourself when you have a labeled dataset and
# want to produce model/weights/model.onnx -- see model/README.md.
#
#   torch==2.4.1
#   torchvision==0.19.1
#   onnx==1.16.2
